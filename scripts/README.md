# Scripts

- `create-labels.sh`: ensures repo labels exist (uses GitHub CLI `gh`).

Usage:
```bash
./scripts/create-labels.sh
```

Requirements:
- `gh` installed and authenticated (`gh auth status`).
