# WS4 Invariants (Draft)

- **INT-01 Authn/authz portability**  
  APIs MUST enforce authentication/authorization with explicit authority mapping and evidence.

- **INT-02 Core resource portability**  
  Core resources (tenant, workload, storage, audit) MUST expose portable operations across providers.

- **INT-03 Standard audit/evidence queries**  
  Audit/evidence query interfaces MUST be standardized and integrity‑protected.

- **INT-04 Evidence export support**  
  Providers MUST support evidence export bundles with declared `evidence_profile_id`.
